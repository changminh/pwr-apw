/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package apw.augmentedLearning.test;

import java.io.File;
import java.util.Hashtable;
//import jpl.Atom;
//import jpl.Compound;
//import jpl.Query;
//import jpl.Term;
//import jpl.Variable;

/**
 *
 * @author Nitric
 */
public class FirstTest {
    public static void main(String[] args) throws Exception {
        // Wczytanie pliku:
        /* Util.wczytajPlik(new File((FirstTest.class.getResource("ntr.pl")).toURI()));

        // Stworzenie zapytania ze zmienną:
        Variable X = new Variable("X");
        // parent(juliet, X);
        Compound c = new Compound("parent", new Term[] {new Atom("juliet"), X});
        Query q4 = new Query(c);

        // Przetworzenie zapytania i wyświetlenie wyników:
        System.out.print("Dzieci Juliet to: ");
        while(q4.hasMoreElements()) {
            Hashtable bindings = (Hashtable) q4.nextElement();
            System.out.print(bindings.get(X.name) + " ");
        } */
        System.out.println("");
    }
}
